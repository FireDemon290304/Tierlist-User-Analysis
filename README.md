# Tierlist-User-Analysis

Do analysis on users using LA

## TODO:

- Make flag for clean sub run in js, so we can append userdata rather than overwrite
- Change fetch flow to be server based
- Parsing Dataclass into mem from "database"
- Functions for linear algrbra analysis
- Make into CLI
